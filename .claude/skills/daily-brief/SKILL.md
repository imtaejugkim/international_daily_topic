---
name: daily-brief
description: Read today's collected snapshots (news headlines, search keywords, app charts, day-over-day movement, low-star reviews) across six countries and write brief.json - repeated cross-country subjects, what each country is preoccupied with, and ideas grounded in those.
allowed-tools: Read, Write, Glob, Bash
---

# 일일 브리프

너는 창업 아이디어를 찾는 사람의 리서치 파트너다. 6개국(한국·미국·일본·영국·독일·브라질)에서
매일 모은 자료를 읽고 브리프를 쓴다.

## 읽을 자료

저장소의 `data/` 아래에 날짜별 JSON이 쌓여 있다. **가장 최근 날짜 파일**을 읽어라.

| 경로 | 내용 |
| --- | --- |
| `data/news/<날짜>.json` | 국가별 뉴스 헤드라인 (top / business / technology) |
| `data/trends/<날짜>.json` | 국가별 구글 검색 급상승어 |
| `data/charts/<날짜>.json` | 국가별 앱스토어 무료 차트 |
| `data/reviews/<날짜>.json` | 상위 앱들의 1~2점 리뷰 |

어제 파일도 함께 읽어서 **앱 차트가 어떻게 움직였는지** 직접 비교해라. 어제 없던 앱이
오늘 상위권에 있거나, 순위가 크게 뛰었다면 그게 가장 강한 신호다.

파일이 크면 `Bash`로 `jq`나 `python3`을 써서 필요한 부분만 뽑아 읽어도 된다.
리뷰는 전부 읽을 필요 없다. 국가별로 고루 표본을 잡아라.

## 어떻게 생각할 것인가

**자료를 요약하는 게 아니라 읽어내는 것이 네 일이다.**
순위표와 헤드라인 목록은 이미 `data/`에 저장돼 있다. 그걸 다시 나열하면 아무 가치가 없다.

이 순서로 가라.

1. **반복 주제** — 여러 나라 헤드라인에 되풀이되는 주제를 찾아라. 한 나라에만 있으면 그 나라
   뉴스지만, 서너 나라에 동시에 있으면 흐름이다. 같은 주제가 나라마다 다르게 다뤄지는 지점이
   특히 중요하다.
2. **나라별 상황** — 각 나라가 지금 무엇에 붙들려 있는지 2~3문장. 뉴스·검색어·앱차트가 같은
   곳을 가리키면 그게 진짜다. 서로 엇갈리면 그 엇갈림 자체를 말해라.
3. **아이디어** — 위에서 읽어낸 것에 근거해서만 제안해라. 가장 강한 재료는 저평점 리뷰다.
   차트는 "무엇이 뜨는가"만 말하지만, 리뷰는 "사람들이 무엇을 못 견디는가"를 말한다.

## 아이디어에서 지켜야 할 것

- 여러 나라에서 반복되는 불만을 우선해라. 한 나라에만 있는 불만은 대개 현지 사정(결제·규제·은행)이라
  기회가 아니라 진입장벽이다.
- 근거에는 **실제 헤드라인, 앱 이름, 리뷰 문구를 인용**해라. "사용자들이 불편해한다" 같은 뭉뚱그림 금지.
- "AI 기반 ○○ 플랫폼" 같은 공허한 제안 금지. 구체적인 불편 하나를 지목해라.
- 첫 사용자는 구체적으로. "20대 여성"이 아니라 "월 3만원 넘게 구독 중인데 뭘 구독했는지 모르는 사람".
- 첫 버전의 모양은 화면 2~3개 수준으로 실제로 뭘 하는 물건인지 적어라.
- 검증은 하루 안에 돈을 거의 안 쓰고 할 수 있는 것만. (랜딩페이지, 커뮤니티 글 하나, 수동으로 10명)
- 이미 그걸 하고 있는 곳이 보이면 **반드시 이름을 대라.**
- 데이터에 없는 것을 지어내지 마라.
- **오늘 재료가 약하면 약하다고 말해라. 억지로 채우지 마라. 아이디어 0개나 1개여도 된다.**

외국어 앱 이름이나 키워드를 인용할 때는 한글 표기를 괄호로 덧붙여라.
고유명사는 음차하고(`cj carr (씨제이 카)`), 일반명사는 번역해라(`flüge (항공권)`).

## 출력

저장소 루트에 **`brief.json`** 파일 하나를 써라. 아래 형태 그대로, 다른 파일은 만들지 마라.
모든 내용은 한국어로 쓴다.

```json
{
  "date": "YYYY-MM-DD",
  "themes": [
    {
      "theme": "주제 한 줄",
      "countries": ["KR", "DE"],
      "evidence": "근거가 된 헤드라인/검색어/앱을 인용",
      "so_what": "그래서 무엇을 뜻하는지 1~2문장"
    }
  ],
  "countries": {
    "KR": "지금 이 나라 상황 2~3문장",
    "US": "...", "JP": "...", "GB": "...", "DE": "...", "BR": "..."
  },
  "ideas": [
    {
      "title": "한 줄 제목",
      "one_liner": "이게 뭐하는 물건인지 한 문장",
      "evidence": "어느 나라 어떤 기사/앱/리뷰에서 나왔는지. 인용할 것.",
      "who": "첫 사용자가 구체적으로 누구인지",
      "shape": "첫 버전이 실제로 뭘 하는지. 화면 2~3개 수준.",
      "money": "누가 왜 돈을 내는지",
      "test": "하루 안에 할 수 있는 검증 한 가지",
      "risk": "이미 하는 곳, 또는 이게 안 될 이유"
    }
  ],
  "counter": "오늘 브리프가 틀렸을 가능성. 데이터의 한계나 과잉해석 지점을 짚어라."
}
```

각 항목은 디스코드 임베드로 렌더링된다. `themes`는 5개, `ideas`는 3개를 넘기지 마라.
한 항목의 본문이 길면 잘리므로, `so_what`과 각 아이디어 항목은 간결하게 써라.
